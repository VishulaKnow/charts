interface ResizingOptions {
    parentEl: HTMLElement;
    elWrapper?: HTMLElement;
    smallestFontSize: number;
    biggestFontSize?: number;
}

export class FontResizer {
    setSize(el: HTMLElement, options: ResizingOptions) {

    }
}